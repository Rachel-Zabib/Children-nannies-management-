<Page x:Class="PLWPF.PageRemove"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PLWPF"
      mc:Ignorable="d" 
      d:DesignHeight="250" d:DesignWidth="300"
      Title="PageRemove" Background="{DynamicResource Brush1}">

    <Grid  HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Please enter your id: " HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Grid.Row="0" Margin="0,50,0,10" />
        <Label Content="ID:" Grid.Row="1" Grid.Column="0"  HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="20" />
        <TextBox x:Name="removeId" VerticalAlignment="Center" Grid.Row="1"  Width="180" MaxLength="9"/>
        <Button x:Name="removeButton" Grid.Row="2"  Grid.ColumnSpan="2" Content="Remove" Width="300" />
    </Grid>
</Page>
