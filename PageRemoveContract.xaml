<Page x:Class="PLWPF.PageRemoveContract"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PLWPF"
      mc:Ignorable="d" 
  d:DesignHeight="250" d:DesignWidth="300"
      Title="PageRemove" Background="{DynamicResource Brush1}">

    <Grid HorizontalAlignment="Center" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Please enter your contract's counter: " HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" Grid.Row="0" Grid.ColumnSpan="2" Margin="0,50,0,0"/>
        <Label Content="Counter:" Grid.Row="1" Grid.Column="0"  HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="15" />
        <TextBox x:Name="removeId" VerticalAlignment="Center" Grid.Row="1"  Width="150" MaxLength="8"  Grid.Column="1"/>
        <Button x:Name="removeButton" Grid.Row="2"  Grid.ColumnSpan="2" Content="Remove" />
    </Grid>
</Page>
